overwrite: true
schema: "http://localhost:4000/graphql"
documents: null
generates:
  lib/__generated__/graphql.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      avoidOptionals: true

